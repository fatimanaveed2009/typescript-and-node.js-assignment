// T-shirt: write a function called make_shirt()that accepts a size and a text of amessage that should be printed on the shirt.
//The function should print sentence summerizing the size of the shirt and the message printed on it.
// Call the function
function make_shirt(size, text) {
    console.log("creating a ".concat(size, " shirt with the message: ").concat(text));
}
make_shirt('large', 'Hello World');
make_shirt('medium', 'Hello World');
make_shirt('small', 'Hello World');
