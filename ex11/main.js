/*assignment#11:NAME:store the name of a few of your friends in array called "name" print each
person`s name by accessing each element in the list, one at a time*/
var names = ["muhid", "fatima", "ahmed"];
//by using loop method
for (var i = 0; i < names.length; i++) {
    console.log(names[i]);
}
